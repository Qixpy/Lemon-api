# Production Environment Variables Template
# Copy this to .env.prod and fill in with STRONG, RANDOM values
# NEVER commit .env.prod to version control!

# Database password (minimum 32 characters)
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_RANDOM_PASSWORD_MIN_32_CHARS

# JWT secrets (minimum 32 characters each)
JWT_ACCESS_SECRET=CHANGE_ME_TO_STRONG_RANDOM_SECRET_MIN_32_CHARS_FOR_ACCESS_TOKEN
REFRESH_TOKEN_SECRET=CHANGE_ME_TO_STRONG_RANDOM_SECRET_MIN_32_CHARS_FOR_REFRESH_TOKEN

# CORS allowed origins (comma-separated, REQUIRED in production)
# For production: https://yourdomain.com,https://www.yourdomain.com
# For local testing: http://localhost:3000
CORS_ORIGINS=https://yourdomain.com

# Optional: Token TTL configuration
# ACCESS_TOKEN_TTL_MINUTES=15
# REFRESH_TOKEN_TTL_DAYS=30

# Optional: Trust proxy (set to "true" if behind reverse proxy/load balancer)
# TRUST_PROXY=true

# ============================================
# How to generate strong secrets:
# ============================================
# Linux/macOS:
#   openssl rand -base64 32
#
# Windows PowerShell:
#   [System.Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#
# Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# ============================================
